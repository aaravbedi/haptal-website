services:
  - type: web
    name: haptal
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: RESEND_API_KEY
        sync: false
